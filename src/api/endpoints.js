const applicationuser = "applicationuser/" ;

const getuserbyid = (id) => {
    return `${applicationuser}${id}` ;
}

const loginendpoint = `${applicationuser}/login` ;
const registerendpoint = `${applicationuser}/register` ;
