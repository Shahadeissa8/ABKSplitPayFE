import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SingleInstallmentScreen = () => {
  return (
    <View>
      <Text>SingleInstallmentScreen</Text>
    </View>
  );
};

export default SingleInstallmentScreen;

const styles = StyleSheet.create({});
